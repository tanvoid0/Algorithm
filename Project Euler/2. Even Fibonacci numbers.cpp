#include <bits/stdc++.h>

using namespace std;

int main(void)
{
    unsigned long long int N, a = 1, b = 2, sum=0, sum2=0, i;

    while (sum2 <= 4000000) {
        if()
    }
    cout << sum2 << endl;

    return 0;
}

