#include <stdio.h>

int main(void)
{
    float a,b,c;
    int i,t;

    for(i=0; i<t; i++) {
        scanf("%f %f %f", &a,&b,&c);
        printf("%f\n", ((a*2)+(b*3)+(c*5))/10);
    }

    return 0;
}
