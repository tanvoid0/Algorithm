#include <bits/stdc++.h>
#include <cstdio>

using namespace std;

int main(void)
{
    int N;

    while((cin>>N)!=eof()) {
        if(N == 0)
            cout<<"vai ter copa!"<<endl;
        else
            cout<<"vai ter duas!"<<endl;
            cin>>N;
    }

    return 0;
}

